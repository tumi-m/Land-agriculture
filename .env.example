# Live listings feed. Any JSON endpoint returning { revision, updatedAt, listings[] }.
# Leave unset to serve the bundled sample dataset.
LAND_DATA_URL=

# Optional bearer token sent with the feed request.
LAND_DATA_TOKEN=

# Shared secret for POST /api/revalidate. Without it the webhook returns 501.
REVALIDATE_SECRET=
