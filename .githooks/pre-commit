#!/bin/sh
# Fast checks only. Build and e2e run in /ship, not here.
# Skip with `git commit --no-verify` when intentional.
echo "pre-commit: typecheck, lint, tests"
npm run typecheck || exit 1
npm run lint || exit 1
npm test || exit 1